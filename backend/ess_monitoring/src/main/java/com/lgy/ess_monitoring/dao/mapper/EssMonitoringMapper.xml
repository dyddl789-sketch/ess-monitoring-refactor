<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lgy.ess_monitoring.dao.EssMonitoringDAO">

    <!-- 특정 디바이스의 최신 데이터 1건 조회 -->
    <select id="getLatestMonitoring"
            parameterType="int"
            resultType="com.lgy.ess_monitoring.dto.EssMonitoringDTO">
        SELECT
            monitor_id,
            device_id,
            voltage,
            current_a,
            soc,
            power_output,
            record_time
        FROM monitoring
        WHERE device_id = #{deviceId}
        ORDER BY record_time DESC
        LIMIT 1
    </select>

    <!-- 회원 기준 모니터링 목록 조회 (device 테이블과 JOIN 필요) -->
    <select id="getMonitoringListByMemberId"
            parameterType="int"
            resultType="com.lgy.ess_monitoring.dto.EssMonitoringDTO">
        SELECT
            m.monitor_id,
            m.device_id,
            m.voltage,
            m.current_a,
            m.soc,
            m.power_output,
            m.record_time
        FROM monitoring m
        JOIN ess_device d
          ON m.device_id = d.device_id
        WHERE d.member_id = #{memberId}
        ORDER BY m.record_time DESC
    </select>

    <!-- 전체 모니터링 데이터 개수 -->
    <select id="getTotalCount"
            resultType="int">
        SELECT COUNT(*)
        FROM monitoring
    </select>

    <!-- 회원별 모니터링 데이터 개수 -->
    <select id="getMemberCount"
            parameterType="int"
            resultType="int">
        SELECT COUNT(*)
        FROM monitoring m
        JOIN ess_device d
          ON m.device_id = d.device_id
        WHERE d.member_id = #{memberId}
    </select>

</mapper>